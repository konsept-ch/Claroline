
import {selectors as cursusSelectors} from '#/plugin/cursus/tools/trainings/store/selectors'

const STORE_NAME = cursusSelectors.STORE_NAME + '.registration'
const LIST_NAME = STORE_NAME + '.list'

export const selectors = {
  STORE_NAME,
  LIST_NAME
}
