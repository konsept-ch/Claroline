# Copy to .env.cep and adjust values
PROJECT=cep
DB_NAME=claroline
DB_USER=clarouser
DB_PASS=claropass
DB_ROOT=rootpass
DB_PORT=3307
DB_VOLUME=cep-db-data
# Must point to an existing SQL file on your host
SEED_SQL=./db/seeds/cep.seed.sql

